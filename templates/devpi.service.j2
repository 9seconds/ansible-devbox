{{ ansible_managed | comment }}

[Unit]
Description=Devpi - caching PyPI mirror

[Service]
ExecStart=/usr/local/bin/devpi-server --serverdir {{ devpi.dir | quote }} --host {{ devpi.host }} --port {{ devpi.local_port }}
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
