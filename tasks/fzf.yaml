---
# vim: set ft=ansible:


- name: clone fzf repository
  git:
    repo: https://github.com/junegunn/fzf.git
    dest: ~/.fzf
    version: master
    clone: yes
  tags: update

- name: install fzf
  command: ./install --all --64
  args:
    chdir: ~/.fzf
    creates: ~/.fzf.zsh
  tags: update
